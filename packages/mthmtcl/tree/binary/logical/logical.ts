import { BinaryNode } from "../binaryNode.ts";

/**
 * Represents a boolean-valued logical operation
 * within the AST.
 *
 * This is a utility class to ensure that all logical
 * operation subclasses are more firmly identifiable and
 * interrelated.
 */
export class Logical extends BinaryNode {
}
